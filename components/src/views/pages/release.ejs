<%- include ('../partials/header') %>
<%- include ('../partials/menu') %>

<main>

  <main>
    <div class="event-card" style="border: 3px solid #000; padding: 20px; margin: 15px; height: 450px; width: 400px;">
        <h1 class="text-centered">Albums</h1>
        <% if (albums) { %>
              <h2><%= albums.name %></h2>
              <img src="<%= albums.images[0].url %>" alt="<%= albums.name %> Image" class="card-img">
    <div>

      <!-- Other album information -->
    </div>
  <% } else { %>
    <p>No album available</p>
  <% } %>


    </div>

</main>

</main>


<%- include ('../partials/footer') %>
